# Morgan setup narration: Source

Status: draft. Regenerate `morgan.mp3` and `morgan.mp4` from this Markdown after review.

## Script

Choose where Morgan should start helping with your code. GitHub or GitLab is the fastest path when your repositories already live there, especially when local Git history points there: approve Morgan, and I’ll detect only the users, orgs, groups, projects, and repos you select. 5D Origin is CTO-managed source for teams that want a private operating layer for agents. We mirror first, then Morgan can run private CodeRun and CI jobs safely inside CTO. Nothing has to migrate on day one. If you later want more sovereignty, lower cost, or private infrastructure, Origin can become the source home on your timeline. Choose Gitea for the lightweight Git server, or GitLab when your team wants GitLab CE and GitLab-style CI workflows.

## Generation notes

- Voice: Morgan via local `voice-bridge` / ElevenLabs voice ID `iP95p4xoKVk53GoZ742B`.
- Audio output: `morgan.mp3`.
- Video output: `morgan.mp4`.
- Video renderer: Scenario **P-Video Avatar / Pruna** (`model_pruna-p-avatar`) with the approved Morgan portrait and current `morgan.mp3` as uploaded source audio.
